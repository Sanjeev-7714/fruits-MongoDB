# MongoDB Connection String
# Replace with your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/fruits-database?retryWrites=true&w=majority

# Server Port
# Render will provide its own PORT, but this is used as a fallback
PORT=10000